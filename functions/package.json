{
  "name": "ownit-functions",
  "description": "Cloud Functions for ownit.miggle.co (RSVP, contribution-to-GitHub-Issues, emails)",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.1075.0",
    "@google-cloud/secret-manager": "^6.2.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "private": true
}
